Coloquem seus codigos no diretorio grafos/strucs

pra testar o front ja feito
se usam o ubuntu

pip install django django-tailwind graphviz

sudo apt install graphviz

pra rodar

python3 manage.py runserver

obs: python 3.8++++